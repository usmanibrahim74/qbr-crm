export default {
  findGroupById(state,id){
    return state.groups.find((group)=>{
      return group.id == data.group_id;
    })
  },
  finItemGroupById(state,id){
    return state.groups.find((group)=>{
      return group.id == data.group_id;
    })
  }

}
